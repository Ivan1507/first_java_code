public class Track extends Transport {
    public Pricep pr;

    public Track(String mark, String model, int max_speed, int weight, int peoples, int max_weight, int current_speed) {
        super(mark, model, max_speed, weight, peoples, max_weight, current_speed);
    }
}
